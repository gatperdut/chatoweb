.row.mt-5
  .col-12.col-md-10.offset-md-1
    .row
      .col-12.col-md-4.col-lg-3(*ngFor="let playerData of playersData")
        .card.text-center
          .card-header {{ playerData.nickname }}
          .card-body
            h5.card-title More Info
            p.card-text Try to sign in again.
